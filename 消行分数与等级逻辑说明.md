# 俄罗斯方块消行分数与等级计算逻辑说明

## 1. 核心逻辑执行顺序
在 `Game::moveDown()` 函数中，当方块无法下移锁定后，消行处理逻辑如下：
```cpp
int lines = board_.clearLines();

if (lines > 0) {
    linesCleared_ += lines;
    // 第一步：计算并增加分数（使用当前未更新的等级）
    static const int SCORES[] = {0, 100, 300, 500, 800};
    score_ += SCORES[lines] * level_;
    // 第二步：更新等级（基于新的总消行数）
    level_ = (linesCleared_ / 10) + 1;
}
```

**执行顺序结论：先计算分数，再更新等级**

---

## 2. 场景验证（用户示例：消4行触发2→3级升级）
### 前提条件
- 当前等级 `level_ = 2`
- 当前总消行数 `linesCleared_ = 19`（距离下一级还差1行）
- 本次消行 `lines = 4`

### 计算过程
1. **总消行数更新**：`linesCleared_ = 19 + 4 = 23`
2. **分数计算（使用升级前的等级2）**：
   - 4行基础分800 × 等级2 = 1600分
   - 总分增加1600分
3. **等级更新**：
   - 23行 ÷ 10 = 2（取整）+ 1 = 3级
   - 等级从2升级为3

---

## 3. 规则总结
- **消行分数计算永远使用消行发生时的当前等级**，即使本次消行刚好触发升级
- 等级升级是在分数计算完成后才执行，不会影响本次消行的得分
- 升级规则：每累计消除10行提升1级，等级 = 总消行数 ÷ 10（取整） + 1
- 基础得分表：1行100分，2行300分，3行500分，4行（Tetris）800分
