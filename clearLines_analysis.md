# Tetris 消行分数与等级更新顺序分析

## 问题背景
在 `game.cpp` 的 `moveDown()` 函数中，消行后会更新分数和等级，但执行顺序直接影响得分计算。

## 关键代码位置
**文件**: `backend/src/game.cpp`  
**函数**: `Game::moveDown()` (第 230-255 行)

## 核心代码片段
```cpp
// 无法下移，锁定方块
board_.lockPiece(currentPiece_);
int lines = board_.clearLines();

if (lines > 0) {
    linesCleared_ += lines;                              // 第245行：先累加消行总数
    // 分数计算：1行100分，2行300分，3行500分，4行800分
    static const int SCORES[] = {0, 100, 300, 500, 800};
    score_ += SCORES[lines] * level_;                    // 第248行：再计算分数（使用旧等级）
    // 每10行升一级
    level_ = (linesCleared_ / 10) + 1;                   // 第250行：最后更新等级
}
```

## 执行顺序详解

### 三步执行顺序：
1. **累加消行总数** → `linesCleared_ += lines`
2. **计算并累加分数** → 使用**当前等级**（升级前的等级）
3. **更新等级** → 根据消行总数计算新等级

### 具体案例分析：
**场景**: 当前等级 2，已消行 18 行，本次消除 4 行

| 步骤 | 变量 | 操作前值 | 操作 | 操作后值 | 说明 |
|------|------|----------|------|----------|------|
| 1 | `linesCleared_` | 18 | +4 | 22 | 消行总数累加 |
| 2 | `score_` | - | +800×2 | +1600 | **使用等级 2 计算** |
| 3 | `level_` | 2 | 22/10+1 | 3 | 等级升级为 3 |

**结论**: 800 分的基数按 **旧等级（2）** 计算，得分为 `800 × 2 = 1600` 分。

## 为什么这样设计？

这是经典俄罗斯方块的标准计分规则：
- 分数反映的是**消除该行时的游戏难度等级**
- 升级是对玩家的奖励，但不影响本次消行的得分
- 下次消行才会享受新等级的分数倍率

## 代码执行流程图
```
消行完成
    ↓
累加消行总数 linesCleared_
    ↓
计算分数: SCORES[lines] × level_  ← 使用当前（旧）等级
    ↓
更新等级: level_ = (linesCleared_ / 10) + 1
    ↓
生成新方块
```
