# Tetris 消行分数与等级计算顺序说明

## 核心代码位置

相关逻辑位于 `backend/src/game.cpp` 的 `moveDown()` 函数中（第 242-251 行）。

## 执行顺序

### 代码流程

```cpp
int lines = board_.clearLines();  // 1. 消除行并返回消除的行数

if (lines > 0) {
    linesCleared_ += lines;                    // 2. 累加消除总行数
    score_ += SCORES[lines] * level_;          // 3. 计算分数（使用当前等级）
    level_ = (linesCleared_ / 10) + 1;         // 4. 更新等级
}
```

### 关键结论

**分数计算在前，等级更新在后！**

## 具体示例分析

### 场景
- 当前等级：2
- 消除行数：4行（正好触发升级：2 → 3）

### 计算过程

1. **消除行前状态**
   - `level_ = 2`
   - 假设之前已消除行数：16行
   - `linesCleared_ = 16`

2. **消除4行后**
   - `linesCleared_ = 16 + 4 = 20`

3. **分数计算**（使用当前等级 2）
   ```
   SCORES[4] = 800
   分数增加 = 800 * level_ = 800 * 2 = 1600
   ```

4. **等级更新**（在分数计算之后）
   ```
   level_ = (20 / 10) + 1 = 2 + 1 = 3
   ```

### 最终结果
- 800 分的基数按 **旧等级（2）** 计算
- 本次得分：1600 分
- 升级后等级：3

## 分数表

| 消除行数 | 基础分数 |
|---------|---------|
| 1行     | 100     |
| 2行     | 300     |
| 3行     | 500     |
| 4行     | 800     |

## 等级计算规则

```
等级 = (总消除行数 / 10) + 1
```

- 0-9行：等级 1
- 10-19行：等级 2
- 20-29行：等级 3
- 以此类推...

## 总结

1. 消行后，**先算分，后升级**
2. 本次消行获得的分数永远按**消行前的等级**计算
3. 即使正好凑够升级的行数，本次分数也不享受新等级的加成
